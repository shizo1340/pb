# Adminbereich
#
# Netcup-/Shared-Hosting freundlich:
# - Kein harter Provider-Pfad zu .htpasswd (würde sonst 500er verursachen)
# - Admin ist per Session-Login in /admin/analytics.php absicherbar
#
# OPTIONAL (empfohlen): HTTP Basic Auth zusätzlich aktivieren
# -> Zeilen unten einkommentieren und AuthUserFile auf deinen absoluten Pfad setzen.
#
#AuthType Basic
#AuthName "Adminbereich"
#AuthUserFile /var/www/vhosts/hosting235358.ae852.netcup.net/privacybasics.de/htpasswd_pbadmin
#Require valid-user

Options -Indexes

# Explicitly deny access to .ht* files (defense in depth)
<FilesMatch "^\.ht">
  Require all denied
</FilesMatch>

# Deny access to htpasswd if you store it here (defense in depth)
<Files ".htpasswd">
  Require all denied
</Files>

<IfModule mod_headers.c>
  Header set X-Robots-Tag "noindex, nofollow"
</IfModule>
